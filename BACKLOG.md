# MemePanel Backlog

> 2026-09-21 起。放「认可方向但暂不做」的项：要么依赖数据模型换代，要么等真实痛点出现。
> 动手前先看 HANDOFF.md 的决策历史，避免翻案已否决的方向（如：常驻后台线程）。

## 功能类

- **「高频」虚拟分组 / 使用频率排序**（路线图旧 v0.7 项的还魂）
  阻塞点：`Entry.used` 现在存的是**最近一次**粘贴的 tick（recency），不是次数。真做频率需要
  新增 `usedCount`（u64）字段 = 数据模型 v5 + 迁移 + paste_entry 计数。产品语义也待定：
  高频 = usedCount ≥ N？top-K？按周衰减？建议等真实库规模上千、用户真的喊"翻不到常用图"再动。
  展示层方案（届时选一）：标签行前置虚拟 chip「高频」/ 最近行扩容可调 / 库排序模式托盘切换。

- **后台导入线程 + 进度条**
  导入目前在 UI 线程同步跑，千张级文件夹导入会冻结面板。真到那个规模再做：
  可复刻 GIF 解码的 worker 线程模式（单 worker + PostMessage 回传 + WM_GIFDONE 同款协议）。
  注意与「轻量级应用不要常驻线程」的用户决策边界：这是**任务期**线程（干完即退），不是常驻监控。

- **导入窗「添加文件夹」换 IFileDialog（FOS_PICKFOLDERS）**
  现用 SHBrowseForFolder（老但能用）。换现代 API 需手写 IFileOpenDialog COM 接口声明
  （无 CRT/ATL，~60 行 vtable 定义）。纯 polish，无功能差异，攒着。

- **thumbs/ 孤儿缩略图清扫**
  哈希名迁移/删除后旧名缩略图可能残留（无害：thumb 路径由 entry 名派生，孤儿永不被引用）。
  可挂在 hash_migrate 尾部一次性清扫，或维护菜单加一项。价值低。

## 决策待用户拍板

- **版本角标去留**（HANDOFF v0.8 遗留"两议"）：角标现在是排查"用户跑的是哪个构建"的关键工具
  （v0.12.5 教训），建议至少保留到 1.0；用户觉得碍眼可只在 -debug 模式显示。

- **设置项组织**（2026-09-21 提出）：托盘菜单已两层深（主题色/不透明度/维护子菜单），
  再加必然三层。方向二选一：托盘菜单分割线+分组重构 vs 独立设置面板（复用 Mgr 管理窗框架，
  标签管理窗同款）。建议后者 + 托盘瘦身成入口集（详见会话结论）。

## 已否决（勿翻案）

- **watch 库目录 / ReadDirectoryChangesW**（2026-09-20 用户否决：轻量级应用不要后台线程）
- **引用式入库 / 按标签分子文件夹 / SHA-256 / EcoQoS**（2026-09-20 导入调研结论）
- **常驻剪贴板监听自动收藏**（噪音+隐私+常驻开销，调研结论）
- **Lightroom 式导入预览确认**（表情包高频碎片入库，确认框是负担）
