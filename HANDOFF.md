# MemePanel 交接文档

> 2026-09-18 创建 · **2026-09-19 更新：v0.7 WebView2 迁移尝试被机器问题阻塞，已回退 GDI 版为当前方案（v0.6.1），用户将新开对话继续 GDI 路线**。本包自包含：源码快照、设计资产、目标 UI 规格、全部决策历史与路径索引。
> **2026-09-19 深夜追加：无头验证管线 + 10 个 GDI 设计变体全部完成（见下方『夜间变体探索』段）。**

## ☀️ 2026-09-19 白天：用户选定 SWISS 为主方向

- **✅ IME 候选窗错位已根治（当晚 10 轮排查结案）**：根因 = 自绘输入框从未应答 `WM_IME_REQUEST/IMR_QUERYCHARPOSITION`（输入法向应用**询问**组合字符屏幕位置的官方协议；输入法 UI「不听广播只等回电」，无应答即退回默认位=窗口原点+(103,117)）。修复 = WndProc 应答该询问（坐标=文本起点+前缀宽+组合串前 dwCharPos 字符宽，附行高/文档矩形，返回 TRUE）。**合并主线时此 handler 必须带上——基线 app/ 同病**。详见 05-swiss/NOTES.md v3.10 段（含 9 轮排查全部无效路径记录与调试方法论：版本角标/启动留痕/run.bat 防新旧进程混淆、-pos 实验+参考十字测坐标 bug）。
- **v3「检视抽屉+横切全套」完成（用户批准方案 B）**：05-swiss「管理模式」整体删除。右键 → 右缘 300px 抽屉（预览/淡化文件名/标签实心-空心 chips 批量打标/+新标签内联输入/索引文字即时保存/武装删除）；Ctrl+点击多选（四角角标）；**数据模型 v2**（Entry.indexText，MP_VERSION 2，v1 无损迁移）；搜索=标签∪索引∪文件名⊕开关（托盘可切，prefs.cfg）；修后缀扫描后静默自动；去重=托盘「维护」→选中+批量抽屉；方向键导航+回车速发；修复 IDM_MGR/IDM_TAG_BASE=2100 同值历史 bug。构建 ~69KB，探针+视觉复核全过。**真机已验证**：右键直开抽屉、多选、托盘菜单/主题色、去重、键盘导航、IME（三输入框焦点路由）。
- **05-swiss v2 精修完成**：删四角文字（标题/计数/文件名/页码，粗黑横梁成为唯一刊头，图片墙 8→9 行）；hover 名字条淡化（14px/8.5px/#555）；**主题色可设置**（托盘「主题色」子菜单 5 预设 + ChooseColorW 自定义（commdlg32 动态加载零新增导入），theme.cfg 持久化（TPT1+u32 RGB），`-accent RRGGBB` 无头验证）。
- **管理模式重设计方案**：`design/edit-redesign-proposals.md`（A 编辑弹窗 / B 检视抽屉（已选）/ C 全上下文化 + 横切设计，每案带 UX 审查）。
- 用户对指令面板的评价：好尝试但信息不够紧凑、不太实用（文档里有紧凑变体思路）。
- **下一步（待用户点头）**：05-swiss 合并回 app/ 主线（store.c v2 + IMR handler + 全部修复一并带回），合并时顺手清理调试遗留（imedef 搬迁等无效代码、版本角标可移除/保留两议）。

## 🌙 夜间变体探索（2026-09-19 深夜自主会话产出）

用户睡前指令：继续打磨 GDI 版，并行探索 ~10 个不同设计。全部完成并验收（10/10 STATUS: DONE）。

**一、基础设施：无头渲染验证模式（已入基线 app/src/main.c）**
- `MemePanel.exe -shot <file.png> [-mgr] [-hover N] [-tag N] [-text 词]`：不显示窗口、不注册热键/托盘/互斥、不模拟输入，与真实显示共用同一条合成管线，DIB 合成到确定性桌面背景后经 WIC 存 PNG。可并行多次运行。
- 无头模式下自动伪造 7 条最近使用记录（让最近行进入构图）；`wic_save_png` 在 wicthumb.c。
- 意义：视觉验证从此**不依赖屏幕截图与合成输入**（用户睡觉时也能迭代）；不受 DPI 陷阱影响。

**二、基线 3 个潜伏 bug 修复（已入 app/src/main.c，58880B，子智能体们交叉发现）**
1. **本机 GDI+ 的 `GdipCreateLineBrushI/F` 传 WrapModeClamp 一律失败**（InvalidParameter/NotImplemented）→ 激活胶囊渐变、hover 名字条遮罩**从未渲染过**（用户从未见过真正的渐变胶囊）。修复：gp.h 增 `GP_WRAP_TILE`，LineBrush 全部换 Tile（两点之间视觉等价）；竖直轴 scrim 改 8 段实心阶梯近似。
2. **`g_footer.left/right` 在 panel_layout 从未赋值** → footer 文件名/管理状态行一直画在零宽/负宽矩形里不可见。已两分支补齐。
3. footer 尾随「·」分隔符（无标签项）已修。

**三、10 个设计变体（variants/，全部自包含可构建，55-65KB）**
- **画廊页（明早先看这个）：`variants/GALLERY.html`**（浏览器打开；基线+10 变体×3 态截图+推荐排序）。
- 每个变体目录 = src + build.bat + out（含统一后的演示数据：42 图 + 动物/猫猫标签）；`NOTES.md` 记录 token/改动/自检/已知不足。
- **第一梯队**：01-halo-refine（琉璃工艺拉满，修 5 个基线 bug，建议进 v0.6.2）· 09-palette（Raycast 式列表布局，唯一信息架构革命，↑↓ 行导航已实现）· 02-liquid（液态玻璃材质最大化）。
- **第二梯队**：04-editorial（杂志版式）· 05-swiss（瑞士网格）· 03-paper-ink（宣纸墨浅色）。**第三梯队**：08-darkroom · 07-candy · 10-retro-pixel · 06-terminal。
- 视觉验收法：PNG 上传 CDN（Read 工具）→ `analyze_image` 视觉模型复核 + 像素探针（每变体自带的 probe.ps1 可复跑）。

**四、编排方法备忘**：10 个 general-purpose 子智能体并行（1 个因账户级限流失败后错峰重启成功）；每变体一份 BRIEF.md（概念/tokens/结构/GDI 实现路径/验收标准）+ 共享 AGENT-GUIDE.md（红线：无窗口运行无输入模拟；GDI 踩坑清单；有界 3 轮循环）。编排状态表：variants/ORCHESTRATION.md。

**五、明早建议流程**：开 GALLERY.html → 挑 2-3 个喜欢的 → 真机跑 `variants/<名>/out/MemePanel.exe -show`（交互/IME/GIF 只有真机能验）→ 决定融合路线（推荐组合：01 的修复 + 09 的布局，或 01+02）→ 把选定变体的 main.c 合并回 app/src/main.c。

## ⚡ 新对话从这里开始（2026-09-19）

- **当前方案 = GDI 版（纯 C + GDI+ 自绘，v0.6.1）**：`main.c` 已恢复为完整 GDI 实现（含琉璃三段式布局/悬停 GIF 动画/管理模式/全部 v0.4.x 修复），构建 55KB 验证可跑。用户下一步想**继续打磨 GDI 版的视觉/交互**。
- **v0.7 WebView2 迁移：代码全部就绪但被本机环境阻塞，已整体存档**（未删除）：`src/main_webview.c`（壳）、`src/webview.cpp/.h`（桥）、`assets/panel.html`（完整琉璃 UI）、`sdk/`（WebView2 SDK+Loader+对照程序）。阻塞原因：本机 WebView2 新会话不渲染（标准微软写法对照程序 `sdk/wvhello.exe` 同样白屏复现；已试 runtime 重装 152→153、--disable-gpu、--disable-features=RendererCodeIntegrity、数据目录换盘、管理员，均无效；本机 Clash Verge(Tauri/WebView2) 反而正常）。**若未来救活（先试重启机器），恢复方法：`cp src/main_webview.c src/main.c` + build.bat 换回 webview.cpp 行（见 git 无，直接看本文档 v0.7 段）。**
- **视觉验收方法升级**：本机已装两个层级的 skills 到全局目录（C:\Users\Traveritas\.agents\skills\，新会话自动加载）：
  - **ui-visual-audit**（自制，从 Codex product-design 提炼）：截图证据先行 + 设计稿 vs 实现并排对比 + P0/P1/P2 分级 + 修复闭环复验；含本机 DPI 物理像素截图法与 MemePanel 已知陷阱。
  - **impeccable / frontend-design / design-taste-frontend**（来自 personal-website/.agents/skills/，用户指路）：设计总监级方法论——brief 优先、"refinement 保留 vs redesign 替换"纪律、craft-floor 质量底线、**有界验证循环**（一轮批量修复+一轮确认即停，防无限打磨烧钱）。注意其 native 审计参考是 iOS/Android 的，Windows GDI 用其原则框架，平台细节以 ui-visual-audit 为准。
  - 建议新对话用法：动手改 UI 前先 `ui-visual-audit` 出一轮带截图证据的 P0-P2 清单 → 按 impeccable 的"批量修一轮+复验一轮"节奏执行。
- 技术栈对比实测（用户决策依据）：MemeManager(WinUI3) 88MB 提交 / FancyText.Desktop(WPF+.NET8) 114MB / WebView2 计划 100-150MB 多进程 / GDI 现版 6-12MB——三个"现代 UI"同量级，GDI 独一档地轻。

---

## 1. 项目定义

**MemePanel（暂名，设计稿代号「即帖 JÌTIĒ」）**：Windows 桌面表情包速发面板。

- 常驻托盘，全局热键 `Ctrl+Shift+.` 呼出悬浮面板
- 浏览 / 搜索 / 标签筛选自己的表情收藏（GIF 为主，也有 JPG/PNG/WebP）
- 点击任意一张 → CF_HDROP 复制 + 自动 Ctrl+V 到当前聊天窗口（QQ/微信/Telegram，保留 GIF 动画）
- 目标：呼出到发送 ≤2 秒；**零依赖单文件 exe（无 CRT、无运行时）**

**核心定位（用户原话共识）**：日常实用工具 + 工程纪律（每个环节用数据验证的快/小），不做 size-coding 行为艺术；但极简≠简陋，UI 要有自己的设计美学——**最终选定「琉璃 HALO」方案**（见 `design/halo-spec.md`）。

## 2. 当前实现状态（v0.4「琉璃 HALO」，源码在 `app/`）

> **2026-09-18 更新：v0.4 琉璃化已完成并验证**（本节下方有验收记录）。

已实现（约 44.5KB exe，MSVC 无 CRT 编译通过，运行验证过）：

| 模块 | 状态 |
|---|---|
| 托盘 + 单实例 + RegisterHotKey（Ctrl+Shift+.，可 -show 参数直开） | ✅ |
| 目录扫描（FindFirstFileExW）→ 内存索引 + index.bin 原子序列化 | ✅ |
| 标签系统：64 位位掩码（筛选 = 微秒级位与） | ✅ |
| WIC 缩略图管线（任意格式→256px JPEG 首帧缓存于 thumbs/） | ✅ |
| **琉璃渲染层：UpdateLayeredWindow 32bpp 预乘 DIB + GDI+ 全合成** | ✅ v0.4 |
| **距离场底图：双层高斯软阴影 + 164° 渐变 + 顶部径向高光 + AA 亮边**（resize 生成一次） | ✅ v0.4 |
| **琉璃布局：检索行 / 最近 7 格数字键标 / 标签胶囊（激活=渐变+柔光）/ 玻璃网格 / 信息行** | ✅ v0.4 |
| **自绘输入框 + 中文 IME（组合串自绘下划线 / 结果串 / caret / 候选窗跟随）** | ✅ v0.4（IME 真机人工验证待做） |
| **hover：微光 + 亮边 + 上浮 2px + 名字条；280ms → 168px 大图浮层（双层软阴影）** | ✅ v0.4 |
| **数字键 1–7 直发最近项；Enter 发送首个可见** | ✅ v0.4 |
| 粘贴链路：CF_HDROP + 恢复焦点 + 40ms 延迟 + SendInput Ctrl+V | ✅ |
| 拖放导入（文件夹自动建同名标签并批量打标） | ✅ |
| 淡入（整帧预乘 alpha 位乘）、托盘图标（琉璃版）、comctl6 清单 | ✅ |

**构建**：`app\build.bat`（vswhere→vcvars64→cl→link，需 VS2022 C++ x64）。产物 `out\MemePanel.exe`。

**性能指标实测（2026-09-18，2048×1280 屏）**：exe 44,544B；隐藏常驻 private ~6.4MB（收起即释放合成表面）；可见 ~19.9MB；热键呼出含表面重建 ~5ms 开销。

**v0.4 验收记录**（对照 halo-spec §5）：玻璃合成/圆角抗锯齿 ✅、软阴影无残影 ✅、激活胶囊渐变+柔光 ✅、hover 微光/名字条 ✅、280ms 大图 ✅、搜索+caret ✅（中文 IME 走标准 WM_IME_COMPOSITION 路径，代码已按 spec 实现，真机 IME 人工验收待做）、热键呼出 ✅、内存 ✅、体积 ✅。测试方法：`MemePanel.exe -show` + PowerShell 截图 + SendInput 模拟（详见会话记录）。

## 3. 决策历史（为什么长这样）

1. 调研确认 MemeManager/EmojiManager 等先例 → 决定做"极致性能+极简"自研
2. 交叉调研（demoscene/传奇工具/最小技术栈/数据管线）→ 定技术栈：纯 C + Win32，无 CRT，WIC 解码，Everything 式内存索引
3. v0.1 功能跑通 → v0.2 深色圆角（被评"无个性"）→ v0.3 贴纸摊新粗野主义（被评"为主题化而主题化"）
4. 用户点破关键：**要的是排版/逻辑层面跳脱 + 参考成熟产品**；多轮 HTML 方案迭代（A-J 共 10 版布局）
5. 转外包给设计 AI 出概念 → 得四案（活字/指令/灯箱/匣）
6. 修正 GDI 能力认知（设计方低估了系统绘图全家桶）→ 增补第五案「琉璃 HALO」= **玻璃浮层 × GDI 真实上限** → **用户定稿**

## 4. 目标 UI：「琉璃 HALO」

完整规格见 **`design/halo-spec.md`**（必读）。一句话：深色半透明玻璃浮层（UpdateLayeredWindow）+ 预计算 alpha 软阴影（AlphaBlend）+ 渐变只给激活态（GradientFill）+ GDI+ 抗锯齿；结构 = 检索行 + 最近 7 格数字键标（1-7 直发）+ 标签胶囊 + 网格 + 悬停大图软阴影浮层。

高保真预览：`design/jiti-design-5.html`（浏览器打开，左侧导航选「05 琉璃」，可交互）。

## 5. 技术要点与踩坑（血泪，勿重蹈）

- **GDI 直写 32bpp DIB 会破坏 alpha 通道**（TextO/BitBlt 把 alpha 写 0）→ v0.4 起矢量/文字/位图全部 GDI+；正确姿势：`GdipCreateBitmapFromScan0(w, h, w*4, PixelFormat32bppPARGB, bmBits)` 包 DIB 像素（零拷贝）+ `GdipGetImageGraphicsContext`，预乘 alpha 全链路正确
- **ULW 模式下绝不调 SetLayeredWindowAttributes**（会切回统一 alpha，丢逐像素）；淡入改为整帧预乘位乘（双路 `0x00FF00FF` 并行乘）
- **flat API 函数名陷阱**：字符串对齐是 `GdipSetStringFormatAlign`/`GdipSetStringFormatLineAlign`（无 ment）；PixelFormat 常量公式 = `序号 | (bpp<<8) | 标志位`（32bppPARGB = 11|0x2000|0xE0000）；绑定声明直接抄 SDK `um\gdiplusflat.h`（26100），别凭记忆
- CreateCompatibleDC 初始选入 1×1 stock 位图；HBITMAP 选在 DC 里时 DeleteObject 静默失败——先换出再删（g_dcStockBmp 判定）
- GDI+ LineBrush 两点是世界坐标：动态位置（胶囊/名字条）按需创建销毁，静态（面板内凹线）没关系
- **HeapReAlloc 不接受 NULL**（≠realloc），首次必须 HeapAlloc——曾致启动崩溃
- **WIC 帧编码序列**：`CreateNewFrame → frame.Initialize(bag) → SetPixelFormat(24bppBGR) → SetSize → WriteSource`；漏 Initialize 静默产出 0 字节 JPEG
- **SDK 26100 把 DROPFILES 移出了 shellapi.h**——main.c 自带等价 typedef（成员全 4 字节，ABI 一致）
- **粘贴必须 CF_HDROP**（CF_DIB 丢 GIF 动画）；粘贴前 20-100ms 焦点落定延迟是承重墙；QQNT 后缀错乱需 32 字节头嗅探（待实现，规划在管理窗口）
- **无 CRT 即无 libm**：sqrt 用魔数初值+牛顿、exp(-t) 用 2^-n 缩放+泰勒（main.c xsqrt/xexpneg）；`_fltused` 需自定义
- **msvc 批处理勿放中文**（cmd 按 GBK 误读）
- 数据格式：index.bin v1 = magic "MPX1" + 版本 + 标签表(≤64, u32长度+UTF-16) + 条目(文件名+tagmask/size/mtime/used 各 u64)；不兼容即整体重建

## 6. GDI 全家桶能力清单（琉璃方案的依据，全部系统 DLL 零依赖）

| 能力 | API |
|---|---|
| 软阴影 / 半透明合成 | msimg32 `AlphaBlend` × 预计算 32bpp alpha 位图 |
| 整窗半透明/异形/抗锯齿边缘 | `UpdateLayeredWindow`（32bpp DIB；注意 EDIT 子控件需自绘输入框或子窗口 layered） |
| 线性/径向渐变 | msimg32 `GradientFill`；或 GDI+ 刷子 |
| 抗锯齿矢量/文字 | GDI+（gdiplus.dll，flat C API，`GdipSetSmoothingMode`）；或 2x 超采样+StretchBlt |
| 点阵/纹理 | `CreatePatternBrush` |
| 发光 | 预渲染径向 alpha 位图 + AlphaBlend（静态，勿追实时参数化） |
| 假磨砂 | 打开瞬间截桌面降采样 + 半透明遮罩 |
| ✘ 真边界 | 每帧实时高斯模糊 / 粒子系统（d2d1.dll 能做但架构变更，不做） |

## 7. 路线图

- ~~**v0.4 = 琉璃化**~~ ✅ **已完成（2026-09-18）**：ULW 窗口架构 + 距离场底图 + 琉璃布局 + 自绘输入框/IME + hover 质感件，全部验收通过（IME 真机人工验收除外）
- ~~**v0.5：视口内 GIF 动画**~~ ✅ **已完成（2026-09-18）**：悬停播放方案（对齐 React 参考的 `playing={hoverId===m.id}`）——网格/最近行保持静态首帧（滚动性能不受影响），仅鼠标悬停的格子与 168px 大图预览播放。实现：`wicthumb.c` 新增 `wic_decode_gif`（逐帧 + 元数据 `/imgdesc/*`、`/grctlext/{Delay,Disposal}`，disposal 2/3 正确处置，每帧存整幅画布快照使播放零合成，delay <20ms clamp→100ms；约束画布 ≤512px、≤64 帧、≤16MB）+ `wic_decode_gif_bg`（后台线程自建 MTA 工厂）；`main.c` 动画状态机（单 worker 线程 + pending 排队 + WM_GIFDONE 回传所有权 + GIF_TIMER 按帧延迟推进），帧在 UI 侧拷入 32bpp DIB 后以 GP_PF_32RGB 零拷贝包装 GpBitmap（与静态缩略图同路）。exe 46KB，播放中 private ~12MB。实测三帧 GIF 红绿蓝轮换 ✓。
  - **踩坑**：①`anim_free` 不能 memset 整个状态结构——want/pending/running 是请求簿记，清掉会让解码完成回调永久错配 + 幽灵请求；②`GdipCreateBitmapFromScan0` 直接包线程产出的 PARGB/ARGB buffer 在本机返回 NULL（未深究），走 CreateDIBSection + 32RGB 路径稳定；③GIF 的 LSD 是 **7 字节**（含背景色索引+像素宽高比），手写 GIF 漏两字节 WIC 直接拒载；④GDI+ (System.Drawing) **不能**保存多帧 GIF——测试 GIF 需手写 GIF89a 二进制（LZW 用 clear-interleave 保持码长不变）。
- **v0.6：管理功能（进行中，主体完成 2026-09-18）**：**同窗口双模式**（不做第二 ULW 表面——右键菜单「管理…」/托盘「管理」/`-mgr` 启动参数进入，Esc 返回速发；窗口在两模式间改变尺寸）。功能：大格（96px）网格 + Ctrl 多选（选中位图）+ 标签胶囊批量打标（选中>0 时点击=按首项方向 toggle）+ F2/按钮重命名（复用自绘输入框，回车提交/Esc 取消，非法字符校验）+ 删除（SHFileOperation 送回收站 + 二次确认按钮武装 3 秒 + **删除按钮固定宽度防武装态 rect 跳动**）+ 去重（size 4096 分桶 → 同 size 组内 FNV-1a 64 流式哈希 → 每组保留首个其余预选中，透明可控）+ QQNT 后缀修正（嗅探 GIF8/PNG/FFD8FF/RIFF-WEBP magic 改扩展名+缩略图迁移）。状态行显示操作反馈（功能性文案）。
  - **已实测**：fixext（fakeqq.jpg→fakeqq.gif ✓）、布局/绘制/点击分发、Esc 分层、-mgr 入口
  - **待人工验收**：去重选中、回收站删除、F2 重命名（自动化验证因测试机在被用户使用而中止——合成输入会打进用户窗口，勿再自动跑）
  - **踩坑**：①`HeapAlloc(HEAP_ZERO_MEMORY)` 的桶头数组零初始化=链尾指向索引 0 → **死循环卡死 UI**（链表哨兵必须显式 -1，memset 0xFF）；②文件名 stem 切分 off-by-one（stem 是点后一位，拷 stem+1 字符会把点带上 → "name..gif"）；③管理按钮组右界必须让开关闭按钮 rect（重叠会让点击被 closeRc 抢先=静默退出管理）
- v0.7（下一步）：ReadDirectoryChangesW 增量同步、使用频率排序/智能"高频"虚拟分组、性能指标自动化测试

**v0.4.1 修复记录（2026-09-18，用户反馈"界面不对劲"后逐区放大排查）**：主面板软阴影被出血边缘硬裁切（出血 60→76px + 远层收紧 dy20/σ18/I0.48，边缘残余 α<0.4%）；大图浮层阴影同样截断（pad 40→60 + dy18/σ15/I0.50）；网格命中检测越界（残余条带点击会误发不可见表情，补 col<g_cols && row<g_rows）；IME 组合期间按键泄漏（WM_KEYDOWN 开头 g_complen 时全部交给输入法）；caret 起点左半被 clip 裁。**阴影参数与出血的约束关系是硬规则：出血边缘处残余 α 必须 <2%，改参数先验算**。

**v0.4.2 根因修复（同日，用户反馈 v0.4.1"一点没修"后定位）**：真正元凶是 `panel_layout` 中 `g_panel` 矩形更新晚于 `rebuild_surface()`——`gen_base` 读到的是**上一次窗口尺寸**的面板矩形，底图被烤成旧尺寸，右下大片透明、内容悬浮（用户看到的怪界面即此）。修复 = 把 `g_panel` 赋值移到 rebuild 之前。v0.4.1 的"验证通过"是假象，原因见下条。**布局类 bug 的教训：先更新数据、再重建依赖它的缓存，顺序是承重墙。**

**⚠️ 验证方法教训（血泪，必读）**：本机是 125% DPI 缩放（物理 2560×1600，DPI 120）。PowerShell 默认是 DPI-unaware 进程，`GetWindowRect`/`FindWindow`/`CopyFromScreen` 全部返回 ×0.8 虚拟化坐标/缩放截图——用它测量 UI 等于拿假地图对真地形。**自动化验证必须先 P/Invoke `SetProcessDPIAware()`**（脚本示例见本次会话），截图用物理分辨率全幅，断言面板矩形 = 窗口原点 + bleed。视觉 AI 复核图片时"看起来对"不等于像素对，最终以程序化像素断言为准。

**v0.4.3 设计变更（2026-09-18，用户决定）**：**删除全部投影阴影**——主面板底图与悬停大图浮层均无 drop shadow，面板四周出血区纯透明。用户实测反馈距离场阴影在浅色桌面上观感不对（灰暗带），调参不满意后选择直接去掉。连带优化：出血 76→3px（仅留圆角 AA 余量），窗口与每帧 DIB 缩小 ~15%；`xexpneg`/`gen_preview_shadow`/`g_prevShadow` 已删，`xsqrt`/`sd_round` 保留（圆角软边仍用）。浮层改为深底(α240)+加亮边(1.2px) 靠对比形成层次。halo-spec.md §2 的阴影 tokens 作废；若日后想恢复阴影，参数公式见 v0.4.1 段（两层高斯距离场，出血边缘残余 α<2% 约束）。

**v0.4.4 打磨（2026-09-18，用户三条反馈）**：①淡入动画掉帧修复——原来动画每帧跑全量合成（memcpy 底图+全部 GDI+ 绘制 10-20ms）撞上 WM_TIMER 15.6ms 精度必掉帧；改为 `panel_show` 合成一次全亮度快照（g_fadeSnap），动画帧只做「快照拷贝+预乘位乘+ULW 提交」(~5ms)，末帧/收起时释放快照。②常驻文案清除（用户：「除了类别按钮别的文字都多余」）——删「最近 · 按 1–7 直接发送」行、搜索框占位文字、Ctrl+Shift+. 热键提示、「N 项」计数、底部信息行默认文案与「HALO · layered 32bpp」；footer 仅悬停时显示「文件名 · 标签」。③搜索栏重设计——白玻璃+蓝放大镜 → 深色 UI 输入框惯例：内凹暗底 ARGB(82,7,9,15) + 10% 细亮边（有输入时转 accent 色）+ 灰白弱化放大镜 + 无占位符（caret 即引导）。

**v0.4.5 重设计（2026-09-18，用户反馈三项）**：①二次唤出闪不透明旧帧——根因是 `SWP_SHOWWINDOW` 先上屏了 ULW 残留的上一会话末帧；修复为**两段式显示**：隐藏态先 `SetWindowPos(尺寸, SWP_NOACTIVATE)` → 重建表面 → 合成 alpha=0 首帧并 ULW 提交 → 再 `SWP_SHOWWINDOW`（显示后补一次 submit 兜底）；淡入从 0 起步、步长 52（~94ms）。②数字键直发删除（用户不喜欢误触语义）——数字回归纯文本输入，最近行数字角标同步删除；最近行仅点击发送。③界面三段式重排：**搜索行 → 标签胶囊行 → 格子阵列**（最近行降为 56px 快捷格、与网格同款样式同间距、空位不画占位），面板尺寸 56%/60% → **46%/54%**（min 700×460 逻辑）、底透明度 0.92/0.88 → 0.94/0.90、空态文案精简为一行。exe 41984→41472B。实测：二次唤出 25ms 采样亮度 202（半透明，无 46 不透明闪烁）；'1' 键不再发送。

**遗留小项**（非阻塞）：IME 真机人工验收；万张库滚动 60fps 压测；测试后 `out/memes` 留有 38 张表情图 + 2 个标签（动物/猫猫）的演示数据。

## 8. 本会话路径索引（原工作区，若仍在）

| 内容 | 路径 |
|---|---|
| 项目主目录 | `C:\Users\Traveritas\.zcode\workspace\default\MemePanel` |
| C 源码（v0.3） | 同上 `\src\`（main.c / store.c / wicthumb.c / util.c + 头 + app.rc/app.manifest） |
| 构建脚本 | 同上 `\build.bat` |
| 设计资产目录 | 同上 `\design\` |
| 五案交互预览（含琉璃） | 同上 `\design\jiti-design-5.html`（=本包 design/ 同名文件） |
| GDI 能力概念页 | 同上 `\design\gdi-power.html` |
| 设计需求文档（历史） | 同上 `\design\ui-brief.md` |
| 设计稿 React 源工程 | 同上 `\design\wemd2\`（npm install && npm run build；本包已快照至 design/wemd2-react-src） |
| 历程布局方案 A-J | 同上 `\design\index.html / refine.html / refine2.html`（历史参考，不必看） |
| 设计 AI 原始交付 | `D:\myDownloads\windows-emoji-manager-design (1).zip`（解压过至 D:\myDownloads\wemd_extract） |
| 交接包（本包） | `D:\Documents\HTA\My Projects\MemePanel` |

## 9. 环境备忘

- 编译：VS2022 Community（`C:\Program Files\Microsoft Visual Studio\2022\Community`，vswhere 可寻）；MinGW gcc 15.2 备用（未用）
- 网络：Clash Verge 系统代理 127.0.0.1:7897 常开；WebFetch 常失败时用 curl -x
- 用户偏好：中文交流；UI 文案中文；批处理文件保持纯 ASCII

## 🚀 v0.8 主线合并完成（2026-09-19 晚）

**05-swiss 已整体合并回 `app/` 主线 = MemePanel v0.8「SWISS」**（68608B，面板右下角常显版本角标 `MemePanel v0.8 b<时间>`）。

- 带回内容：瑞士视觉全套（v2 精修）+ 检视抽屉 + 数据模型 v2（index.bin MP_VERSION 2，v1 无损迁移）+ 搜索语义（标签∪索引∪文件名⊕开关）+ 主题色系统（theme.cfg）+ **IME 根治（IMR_QUERYCHARPOSITION 应答）** + NULL-owner 菜单修复 + 修后缀静默自动 + 键盘导航 + `-shot` 无头验证 + `-imedebug` 仪表 + run.bat/run-dbg.bat。
- 合并时清理：删除实测无效的「默认 IME 窗搬迁」代码（保留注释指路真修复）；其余排查期设施（版本角标/启动留痕日志/隐形 caret/CFS 调用）保留。
- 数据迁移：variants/05-swiss/out 的 index.bin（v2，含标签与使用记录）/ theme.cfg（当前默认正红）/ prefs.cfg 已复制到 app/out。**注意：用户真机选过的钴蓝在此前排查中已被重置为默认红，喜欢可托盘再选。**
- app/src 的 WebView2 存档（main_webview.c/webview.cpp/h）与 v0.3 legacy 均未动；variants/ 全部 10 个设计目录保留作设计档案（画廊页 GALLERY.html）。
- 启动方式：`app\run.bat`（正常）或 `app\out\MemePanel.exe -show`；热键 Ctrl+Shift+. 照旧。

## 🖱️ v0.11 体验三件套（2026-09-19 傍晚）

**可拖动 + 智能呼出定位 + 窗口不透明度**，全部真机自动化冒烟通过（`app\smoke_v11.ps1`，四断言：光标锚定±1px / Ctrl+滚轮落盘 / 拖动记忆位精确 / 重呼出沿用+工作区夹取）。

- **窗口拖动**：主面板死区 = 发丝线以上整带（搜索框/✕/标签 chips 除外，hover 显十字移动光标），`WM_LBUTTONDOWN → SendMessage(WM_NCLBUTTONDOWN, HTCAPTION)` 借系统模态移动循环——原生手感、拖动零重绘（分层窗内容随窗移动）。标签管理/导入窗口 = 刊头带（标题区，✕ 除外）同款，不记位置。
- **智能定位**（`panel_show`，热键/托盘共用）：①有拖动记忆位且**记忆中心与光标同屏** → 沿用并夹回工作区（分辨率/DPI 变更防出界）；换屏或无记忆 → ②光标锚定：水平以光标居中、光标落在面板上 40% 线，整面夹回工作区（取代 v0.10 的固定 1/5 高度居中）。`-pos X,Y` 实验覆盖保留（走同一记忆位通道）。
- **不透明度**：三窗（面板/标签/导入）统一，经 ULW `SourceConstantAlpha=g_opacity` 生效——预乘位图整体再乘 op/255，与逐像素乘等价且**零遍历成本**，与淡入动画天然复合（fade×op）。调节：面板/管理窗 Ctrl+滚轮（±13/档，下限 110≈43%）+ 托盘子菜单 100–50% 六档；面板左下角 900ms 读数 HUD（8.5px，同版本角标款）。无头 -shot 不经 ULW，探针确定性天然保住（tags/imp 截图逐像素一致已验证）。
- **prefs.cfg v3（'MPP3' 24B）**：v2 + posX/posY（POS_NONE=0xFFFFFFFF 哨兵）+ opacity；v1/v2 兼容加载自然升版。
- 冒烟方法论沉淀：①PowerShell 测试进程必须 `SetProcessDPIAware()`，否则坐标被虚拟化 1/1.5 缩放、所有窗口位置断言失真（本次最大弯路）；②ps1 内含中文注释必须存 UTF-8 **带 BOM**，PS5.1 无 BOM 按 GBK 误读可吞行；③本机 `FindWindow(cls,$null)` 不匹配（与文档相悖），须类名+标题双参；④合成拖动必须真实输入（`mouse_event` 按下/移动/抬起），PostMessage 的 WM_LBUTTONDOWN 不改物理键状态、模态循环立即退出。

**v0.11.1 跟进（2026-09-19 晚，用户两反馈）**：①托盘新增「呼出跟随光标」checkable（prefs flags bit2 = PF_FOLLOWCURSOR）：勾 = 每次呼出都光标锚定（忽略拖动记忆位）；不勾 = 默认记忆位优先。`-pos` CLI 仍最高优先（g_cliPos 标志，保实验/探针确定性）。②「不透明度 100% 仍透 4%」根因 = 面板底色常数 rgba(250,250,249,0.96)（v0.8 玻璃设计）而非 ULW alpha；修复 = 满档（g_opacity==255）时 gen_base/mgr_base 的底 alpha 245→255（预乘两套），opacity_set 检测跨界即时重烤主面板底图缓存（管理窗每帧重画底自动生效；面板隐藏时下次 show 自然新值）。无头像素验证：100% 顶带 (250,250,249) 零背景混入，90% 档 (242,242,242)。冒烟 ⑤（跟随模式锚定断言）已加，五断言全过；真机注意：面板定位在 panel_show 瞬间取光标，冒烟须「隐藏→设光标→紧贴呼出」压缩用户鼠标干扰窗口。

## ⌨️ v0.12 纯键盘全覆盖（2026-09-19 晚）

主界面全操作可纯键盘完成（99840B，smoke_kbd.ps1 六段全过）。**键盘地图**：
- **动作键**（面向「当前键盘选中对象」= 抽屉目标 > 网格选中框）：`Ctrl+I`/VK_APPS 开·重指向抽屉（=右键）；`Ctrl+D` 收藏；`Ctrl+Space` 多选 toggle（=Ctrl+点击）；`Ctrl+Delete` 武装删除两段式（未开抽屉时先开看清对象再武装）。
- **空间导航**：↑↓←→ 网格导航（已有）+ 首行↑/首格← **贯通进标签行**（tag_focus_entry 落在当前激活 chip）；PgUp/PgDn 翻页；键盘选中也挂 PREVIEW_TMR（280ms 大图，对齐鼠标 hover）。
- **Tab 焦点循环**：抽屉关 = 搜索↔标签行（g_tagFocus）；抽屉开 = 搜索→索引框→chips（g_chipFocus）→新标签(展开时)→搜索；批量态自动跳过索引框。
- **两级焦点态**：g_tagFocus/g_chipFocus（下标制）；←→/Enter/空格 语义与鼠标点击同源（复用 refilter/drawer_toggle_tag）；焦点可视化并入 draw_chip/draw_dchip 的 hover 高亮（零新视觉）；焦点态打字 = 自动回搜索框继续输入（WM_CHAR 复位）。
- **Esc 分层**：chips→索引框（批量回搜索）→标签行回搜索→抽屉输入回搜索→关抽屉→隐藏。**教训：前置焦点块 return 0 会吞 Esc——分层 Esc 必须置于所有前置块之前**（首版 bug，冒烟 k4 抓到）。
- 其他：空库 Enter=开导入窗（对齐鼠标）；网格态 Ctrl+D 后**选中框跟随同一张图**（refilter 收藏重排防二次 Ctrl+D 误伤——真 bug，冒烟 k5 抓到）；WM_CHAR 挡 Ctrl 组合字符（Ctrl+Space 的空格不进搜索词）；鼠标点击时键盘焦点让位；抽屉关/面板隐藏时焦点复位（drawer_set_open/drawer_reset_state）。
- **真机键盘冒烟方法论（smoke_kbd.ps1）**：①动作键用 GetKeyState 查 Ctrl → 必须真实键盘（keybd_event），PostMessage 不设键盘状态；②面板必须前台——**后台脚本 SetForegroundWindow 被前台锁拒（AttachThreadInput+ALT 也常失败），正确路径 = keybd_event 注入真实全局热键 Ctrl+Shift+.**，系统把输入投给面板进程 → 面板获得前台权限 → panel_show 抢前台成功（即真实用户路径，可断言 GetForegroundWindow==面板）；③断言面板状态用窗口宽度（抽屉开 = +S(300)@125%DPI=+375）与 IsWindowVisible；④写库断言用 index.bin LastWriteTime（对称 toggle 还原 + finally 字节级备份）。
- **DPI 勘误**：本机实为 **125%**（2560×1600 物理 → S(x)=1.25x，S(300)=375 实测）；v0.11 记忆里写 150% 是误判。

**v0.12.1 跟进（2026-09-19 晚，用户两反馈）**：①「激活标签 chip 键盘焦点零反馈」——焦点下划线原本只画在非激活分支（黑底下黑线不可见所以被跳过）；修复 = 激活/已打标（黑底）chip 上的键盘焦点画**底边 2px accent 红线**（draw_chip/draw_dchip 同款）。②「最近行键盘到不了」——新增 **g_recentFocus 焦点态**：空间模型完全贯通 标签行 → 最近行 → 网格（首行 ↑ 列对齐落最近行、再 ↑ 到标签行；标签行 ↓ 先落最近行；←→ 跳空格；**Enter 即发最近表情**——快捷发送即"到达+Enter"，未加独立快捷键避免占用可打印字符）；焦点视觉 = 红框+编号+名字条（与鼠标 hover 同款）。无头验收新旗标：`-kbdtag`/`-kbdrecent` 预置焦点态截图（视觉模型确认红线/红框/编号/名字条全部到位）；冒烟 k7：↓↑→Enter 全链路（面板隐藏 + 剪贴板 CF_HDROP 断言）。Tab 循环不含最近行（空间导航专属）。

**v0.12.2 跟进（2026-09-19 晚，用户两反馈）**：①「焦点红线不显眼」——弃底边细线，键盘焦点统一升级为**外侧 2px accent 红框**（draw_red_frame 复用，与网格选中框同语言；黑底激活 chip/白底普通 chip/抽屉 chips 全部同款）。鼠标 hover 仍是下划线——两套输入通道视觉各表。②「进最近行后网格选中框双焦点并存」——**单一焦点原则**：kbd_row_focus()（g_tagFocus/g_recentFocus 任一激活）时 draw_grid 的 hov 与 draw_preview 停画（g_hover/g_previewIdx 内存保留，↓ 回网格原位恢复；大图同停避免三焦点）。无头验证：`-kbdtag` 外框完整（视觉模型确认含红方块整体被包）；`-hover 0 -kbdrecent` 互斥组合 = 4.7% 差异（大图 28k px + 网格框消失 + 焦点框出现），最终图网格零红框、无大图。

**v0.12.3 跟进（用户定稿）**：键盘焦点样式三轮收敛——v0.12.1 底边 2px 红线（嫌不显眼）→ v0.12.2 外侧红框（嫌丑）→ **定稿 = chip 下缘外侧 3px accent 线**（draw_chip/draw_dchip 同款）：比 hover 的 2px 黑下划线粗一档、颜色区分，激活黑底上画在块外黑白红分明；单一焦点互斥（kbd_row_focus 停画网格框/大图）保留。-kbdtag 无头复核：3px 红线完整横贯整块、无外框。

**v0.12.4 微调（用户）**：焦点线嵌回 chip 底边**内侧**（v0.12.3 画到块外被指出回归——对齐 v0.12.1 的内嵌位，保持 3px 粗细；draw_chip/draw_dchip 同步）。-kbdtag 复核：红线完整嵌于黑块内、不外溢。

**v0.12.5 修正（用户「还是没改对」）**：先验证用户实例确为最新构建（进程启动 20:18:11 vs exe 20:17:53 + imedbg.log 吻合）——真没改对：激活 chip 的 hit 含**黑块外左侧的前置红方块**，v0.12.4 焦点线画整 hit 宽 → 左端一截落在黑块外白纸上（视觉模型上轮答"含红方块横贯"被误当正确）。修正 = 激活态线只画黑块段（bl..bl+bw，红方块下不画）；普通态仍全宽。**版本角标升级为带小版本**（v0.12.1~.4 角标一直显示 "v0.12" 只有构建时间差异，用户无法辨新旧是本轮困惑放大器——今后每次微调必须同步角标小版本号）。-kbdtag 复核：红线左端从黑块左缘起、红方块下方无线、角标 v0.12.5。

## ⌨️ v0.13 键盘动效 + Tab 抽屉开合（2026-09-19 晚）

**①键盘焦点驱动 GIF 动画**（用户反馈：焦点时 GIF 不播、鼠标悬停可以）：anim_request 全路径接线——hover_move 移动后 request 当前格（与鼠标 WM_MOUSEMOVE 同源）；最近行焦点 ←→ request 对应格；贯通进标签行 request(-1)（网格框停画动画同停）；↓/Esc/打字复位回网格时 request(hover 格) 恢复；大图浮层动画同路径自动生效。
**②Tab = 抽屉展开/收起**（用户提议，Tab 原焦点循环退役——标签行本有 ↑/← 贯通入口）：抽屉关 → Tab 打开（目标 = 键盘选中格，对齐 Ctrl+I）；开 → Tab 收起。**键盘打开（Tab/Ctrl+I）单目标即落索引框焦点**（编辑上下文，含 IME 位同步；批量留搜索）。抽屉内纯空间模型：索引框 ↑ = 进 chips（落已打标项）、chips ↓ = 回索引框（批量回搜索）、Enter 打标、Esc 逐层退。冒烟 k3 改新路径（Ctrl+I→↑→Enter）、k8 = Tab 开合（宽 1185→1560→1185）、k6 需三连 Esc——八段全过。教训：k6 曾因测试脚本没跟上 Tab 新语义误报（两 Esc 不够）。脚本 AttachThreadInput 导入曾被误锚回 kernel32（Fg 早退掩盖，前台丢失时才炸）——改导入段时整段核对。

## 📥 v0.14 导入内容寻址（2026-09-20）

**调研 → 决策**：导入管理两轮调研（业界四模型 A Lightroom 导入会话 / B Eagle-Billfish 托管+监控 / C 剪贴板捕获 / D Everything 引用；GitHub `shuiping233/MemeManager`（WinUI3）源码精读——其 sha256 内容寻址 + 三阶段并行导入 + watch 自库 + 按分类判重）。用户拍板：**采纳内容寻址但全局化**（MemePanel 标签=横切位掩码，无需像 MemeManager 按分类判重=被迫复制）、不做 watch 自库/后台线程（轻量级应用不要常驻线程，用户明确否决）、不做引用模式/子文件夹/SHA-256/EcoQoS。调研结论存会话记录；克隆 `%TEMP%\mm-research` 可复核（关键文件 MemeDataEngine.cs/FileWatcher.cs/ImageDragHelper.cs）。

**实现（exe 104960B，构建一次过；`app\smoke_imp.ps1` 21 断言全过 + 视觉复核）**：

- **数据模型 v4**（MP_VERSION 4，v1/2/3 兼容加载）：Entry 追加 `hash`（u64，内容 FNV-1a 64，0=未知）+ `origName`（显示/搜索用原名，NULL=回退磁盘名）。盘面顺序 name,tagmask,size,mtime,used,indexText,flags,**hash,origName**。`fnv_file` 从 main.c 移入 store.c（导入/扫描/迁移/去重共用）；新增 `entry_disp_name(e)` = origName 优先。**搜索的"文件名"语义、网格/最近行名字条、抽屉元数据列全部改读 disp 名**——用户观感不变，但磁盘上全是哈希名。
- **copy_into 重写 = 入库唯一门**：①读 12 字节魔数定真实格式（GIF8/PNG/JPG/RIFF-WEBP；**BMP 不进魔数表**——"BM" 仅 2 字节，BMW….txt 会误命中，BMP 只走扩展名白名单兜底）；魔数不认再看白名单（png/jpg/jpeg归一jpg/gif/webp/bmp），都不认=非图片源头拒收。②`fnv_file` 算哈希。③判重双路：索引内同 hash+同 size（挡碰撞+能手放文件命中）→ dup；磁盘同哈希名已存在（孤儿/同批同内容）→ dup，**天然幂等**。④复制为 `16位小写hex哈希+真实扩展名`（hex 手写移位，不赌 wnsprintf 的 64 位格式符）。返回 0新/1重复/2非图片/3失败。QQNT 错后缀文件导入即落真实扩展名，fixext 对新导入基本失业（旧文件仍管）。
- **import_run 重写**：新增/重复/非图片三计数；状态行+气泡 =「新增 %d · 重复 %d · 非图片 %d · 库共 %d」（zh/en）；origName 写回（重复件保留既有原名，先到先得）；文件夹标签**惰性创建**（空文件夹/纯非图片不再留空标签）；got 数组动态扩容——**顺带修掉 v0.10 老 bug：文件夹导入只给第一个文件打标签**（原 `nGot<n` 用路径数当容量，拖一个文件夹 n=1，50 张图只有第 1 张吃到标签）。
- **hash_migrate（启动一次性，store_init 后、scan 前）**：触发条件=任一条目 hash 未知**或**文件名非哈希名（覆盖手放文件/迁移改一半），幂等收敛。流程：备份 index.bin.bak → ①补哈希（死条目清；**已知哈希的先试哈希名找回**——迁移改名中途崩溃的自愈）→ ②同哈希合并（保序留首项；tagmask/flags 取或、used 取大、indexText/origName 取首个非空；多余文件+缩略图删除——**这一步就是全库去重**）→ ③改哈希名+缩略图随迁+origName=旧名（hash==0 跳过改名防"000..名"，下轮重试）。fixext 随后纠 ext（哈希段不动身份不变）。
- **粘贴导入（CF_DIB→PNG）**：先落 `~paste_%u.png` 临时名再哈希收编（dup=删临时件），与拖放同一条内容寻址纪律——同一截图贴两次只存一份。
- **store_scan**：新文件/内容变更条目即时补哈希（判重要查它；拇指生成本就读全文件，代价可忽略）。
- **`-import <路径>` 无头钩**（文件或文件夹，含空格加引号；-shot 前执行）：导入行为确定性验证入口。**smoke_imp.ps1**（纯 ASCII，沙箱 %TEMP%\mp_smoke14，不碰真实库）：手工构造 v3 索引二进制 → 断言迁移（v4/哈希名/合并/tagmask·used·flags·indexText 保真/.bak/fixext 纠 .gif/缩略图/二次运行幂等不重备份）→ -import 断言（新 2+重复 1+非图片拒收/子文件夹忽略/origName 先到先得/文件夹标签打到每个新件/再导幂等）→ 截图视觉复核（v0.14 角标 + 名字条 indexText 优先 + 状态行计数）。**PowerShell 解析 index.bin 的读写器已沉淀在脚本里**（Read-Index/Write-IndexV3，后续改格式可直接复用）。
- **测试教训**：演示库 42 文件仅 12 种内容（capybara×6、hamster×4…）——选源图必须按 Get-FileHash 去重，否则"恰好同内容"把断言全搅浑（首次 21 断言挂 9 个全是这个）；孤儿 tag 位（手工索引造的空槽位）会被 store_tag_add 的"首个空槽"认领——真实路径不会出现（删标签清位），但造测试数据要用真实存在的位。
- **已知取舍**：索引丢失后重导入同文件会走"磁盘判重"路径命中 dup（文件名幂等），但其标签无法回挂到孤儿条目（条目由 scan 补建，无标签）——罕见，接受；「打开库文件夹」里全是哈希名（Explorer 可读性换全局唯一性，origName 在面板侧展示）——用户已知情采纳。
- **真机验收（2026-09-21 用户）**：v0.14 通过 + smoke_kbd.ps1 跑完无异常。

## ✏️ v0.15 F2 重命名 + 去重零 IO（2026-09-21）

用户拍板「小而容易的先做，难项进 backlog」→ **BACKLOG.md 建档**（高频分组需 usedCount 字段=数据模型 v5、后台导入线程、IFileDialog、孤儿缩略图清扫、版本角标去留、设置面板方向；另记录已否决方向防翻案）。

- **抽屉 F2 重命名**：v0.14 白送的能力——改名 = 纯 `origName` 元数据编辑，零磁盘扰动。实现：元数据列名称行从静态淡化文字升级为**输入框**（`g_dNameRc` 复用为框 rect，S(28)，size/open/fav 顺移 +S(16)）；第五编辑缓冲 `g_ebR` + `EDIT_NAME` 焦点态全接线（edit_buf/edit_changed/IME 定位/点击命中/Esc 泛化路径自动覆盖）；**name_commit 与 index_commit 同款即时写回**（三分支 ReAlloc，空 = 清改名回退哈希名）；`drawer_show_single/batch` 装载/清空。**键盘**：F2 = 单目标抽屉（未开则开键盘选中格）落名称框（批量态忽略）；名称框 ↑=回搜索、↓=进索引框、Enter=回搜索。真机待验：F2 手感 + IME 中文改名。
- **去重扫描零 IO**：`maint_dedup` 改为 hash 字段纯内存比较（v0.14 起常驻索引），万张库从全库读盘降到毫秒级位比较；size 分桶保留（防哈希碰撞双保险）；hash==0 条目跳过（无法判定）。
- **`-rename 词` 无头钩**（需配合 `-drawer N`）：经 g_ebR→name_commit 真链路改名落盘 + 构图聚焦态。沙箱验证：origName 落盘断言过 + 视觉复核（名称框聚焦下划线/布局无重叠/标签索引节正常）。
- **踩坑**：`-drawer N` 的 `if (k<=0) k=5` 把 0 当缺省回退 5——「-drawer 0」实际指向第 5（钳尾）项，测试断言查错条目（改名本身是对的）。既有行为未改，测试用 N≥1。

## ⚙️ v0.16 设置窗口 + 托盘瘦身（2026-09-21）

用户采纳建议：**独立设置面板（复用 Mgr 管理窗框架），托盘瘦身为入口集**——不做右键菜单分割优化（线性菜单放不下还会再涨的偏好项；设置是低频操作应收进面板）。

- **设置窗**（`MemePanelSet` 类，S(420)×S(560)，与标签/导入窗同款 mgr_base/chrome/swap/ulw 框架，刊头可拖、Esc 关闭、Ctrl+滚轮不透明度同款）：三分区——**外观**（主题色 5 swatch 当前带框 + 自定义…→ChooseColorW；不透明度六档 chips 激活=黑实底；最近行格数 1..7 chips）·**行为**（语言/开机自启/呼出跟随光标/搜索包含文件名，四开关行：右端 16px 方块实心=开细框=关，行 hover=标签下划线）·**库**（重新扫描/去重扫描…/标签管理…三按钮）。全部动作复用既有函数（theme_apply/opacity_set/autostart_set/maint_dedup/set_rescan 同旧托盘路径）；语言切换即时生效并刷所有可见窗。**无文本输入 → 无 IME**。
- **托盘瘦身后形态**：打开面板 / 导入… / 设置… / ─ / 呼出跟随光标(checkable) / ─ / 退出。主题色/不透明度/语言/自启/最近行/文件名搜索/重扫/去重/标签管理全部只余设置窗入口。旧 IDM 派发分支整体删除。
- **`-win settings` 无头钩** + 顺修两处既有问题：① `-accent`/`-lang` 解析挪到 `-win` 之前（此前 -win 提前 return，管理窗渲染吃不到覆盖——两张图字节相同暴露的）；② ImportProc 的 ✕ 悬停死 bug（mgr_chrome 读 g_tMouse，导入窗只更新 i_mouse）——i_mouse 回写 g_tMouse，设置窗同款处理。
- **验证**：构建 111104B；`-win settings` zh/en 双语无头渲染 + 视觉复核（分区/swatch 选中框/激活 chips/开关态/三按钮/无重叠越界全过）。**真机待验**：托盘菜单新形态、设置窗各控件点击、语言切换、自定义取色。
- **取舍记录**：标签管理从托盘移除（入口=设置窗库区）；若日常觉得绕可加回托盘（一行 AppendMenuW）。

## ⚙️ v0.16.1 设置窗五项修补（2026-09-21，用户真机反馈）

1. **最近行第 7 格越界**：首版 chips 左起 `S(170)` + 每块 text+20/gap8，总宽估错溢出面板——改为**量总宽后右对齐**（`R - tot`），永不越界。教训：视觉模型上轮报"无越界"是漏检（激活块之外的小块贴边它不敏感）——布局类改动要像素断言或问"最右一块"。
2. **范围两端补全**：chips 从 1..7 扩为 **0..10**（0=关最近行；面板默认列数 9 之内可选满第一行）。`panel_layout` 同步收带：`g_grid.top = wallT + (g_recentN ? g_cell+g_gap : 0)`——关掉后图片墙直通发丝线不留空档；prefs 夹取上限 7→15（0 合法化）。
3. **不透明度下探**：六档(100..50%) → **八档 100..30%**（+40%/30%），`OPACITY_MIN` 110→76。不做字面 0%：面板全透=不可见不可操作，热键呼出后像软件坏掉（30% 已是可用性下限；用户坚持可一行改）。
4. **激活态 accent 化**：chips 激活底色、开关方块实心、swatch 当前项描边全部黑→**主题色**（对齐"accent 只给激活态"的面板语言）；hover 仍黑色加粗（两套输入通道视觉各表）。
5. **去重点击"没反应"**：真相=**v0.14 迁移已把库去重干净**，marked=0 只发托盘气泡，被系统通知设置吞掉。`maint_dedup` 改返回预选数，设置窗底部加**状态行**（导入窗同款）就地显示「已选 N 张疑似重复——在面板抽屉里删除 / 未发现重复」；重扫同样写状态行。顺手：百分比显示整数截断 49%/29% → 四舍五入 50%/30%（旧公式就有的毛病）。

构建 111616B；无头验证：设置窗（11 块右对齐"10"完整在面板内/8 档全在内/激活态红色/开关态与实际一致）+ recentN=0 面板收带渲染。**真机待验**：五项对应操作 + 30% 档观感。

## 🔧 v0.16.2 最近行定版 + 越界闪退根修（2026-09-21，用户真机反馈）

**闪退根因（血泪）**：v0.16.1 设置窗把最近行档位放到 0..10，但 `g_recent[7]/g_recentIdx[7]/g_recentImg[7]` 数组按旧上限 7 声明——选 8/9/10 时 `panel_layout` 越界写 `g_recent[7..9]`，正好踩掉紧随其后的 `g_recentIdx` 与 `g_nRecent`（RECT 16 字节 × 3 跨过 int 数组），全局态损坏 → 空白格点击即崩。**教训：任何"档位上限"设置项落地前，先核对其写入的全部数组边界——上限提升三处（数组/prefs 夹取/UI 档数）必须同改。**

**用户定版（简化方向）**：不再调数量——最近行**固定一行**（槽位=网格列数，随面板宽自适应，封顶 16），**满了轮换**（recalc 按使用时间排序天然顶掉最旧），设置只留**开/关**。

- 数组统一 `MP_RECENT_MAX 16`；`g_recentOn`（BOOL，prefs 里 recentN 非 0=开、0=关，旧文件 1..15 兼容映射；盘面存 7/0）。
- `g_recentN` 降级为**派生值**：`panel_layout` 内 = `开 ? min(g_cols,16) : 0`，任何地方不得手写。
- **panel_layout 末尾统一 `recalc_recents()`**：修一个顺序缺陷——布局改列数（拖宽面板/抽屉开合/无头渲染）后最近行不重算，尾部槽位空置（真机与无头同病）。
- `recalc_recents` 尾槽消毒：未填到的槽位统一 -1，杜绝陈旧索引被点击/悬停命中（空白格闪退的第二重保险）。
- 设置窗：数字块退役，「最近行」= 行为区第 5 个开关行；无头伪造最近记录伪满全部槽位（MP_RECENT_MAX）。
- 验证：设置窗（5 开关行/最近行红实心/数字块已删）+ 面板（最近行 13 格全满与图片墙 13 列一一对齐）。构建 111104B。**真机待验**：开关切换、拖宽面板槽位跟随、发几张图看轮换。

## 🖱️ v0.16.3 最近行 hover 修复（2026-09-21，用户真机反馈）

- **行内滑动不刷新**：`WM_MOUSEMOVE` 的重绘条件只有 `hv != g_hover`（网格 hover 变化）——鼠标在最近行内横向滑动时 `cell_from_point` 恒返回 -1，条件永假、`panel_repaint` 永不触发，焦点框卡在"进入行的那一格"，其余格怎么悬都不亮；键盘没事因为按键路径每次都重绘。修复：新增 `g_recentHover`（hover 槽位），`hv != g_hover || rv != g_recentHover` 才重绘；`WM_MOUSELEAVE` 同步复位。教训：**自绘 hover 状态机里每类可 hover 区域都要有独立的"变化检测"，共享网格的 hv 对行内移动是盲的。**
- **勘误 v0.16.2**：「拖宽面板槽位跟随」验收项作废——面板是 WS_POPUP 固定尺寸（呼出按屏幕 46%×54% 定），用户无法拉宽；列数实际只随分辨率/多屏变化而变，panel_layout 末尾的 recalc 兜的是这条路径（保留）。

## 🎨 v0.17 三项视觉修（2026-09-21，用户反馈）

1. **网格末行分割线越界**：旧 `draw_grid` 的列间竖线一律画到 `wallB`（末行底），末行没填满时线条穿透空白格直奔面板右缘。修：`lastFill = shown - lastRow*g_cols`（末行实格数），`upB` = 上一行的行缝高度，`c < lastFill` 的列画到 `wallB`、其余只画到 `upB`；`lastRow==0` 时整列空的 `upB=0`，用 `b > g_grid.top` 防负高矩形。语义与最近行对齐——线只存在于"两格都有内容"的缝里。
2. **导入窗统计文本与按钮排重叠**：`imp_layout` 里统计文本在 `panel.top+S(50)..S(66)`，按钮排却从 `S(14)` 起——同带冲突。修：按钮排起点 `S(14)→S(34)`（统计文本之下留净空）。
3. **标签窗可滚动但没人知道**：TagsProc 本来就有 WM_MOUSEWHEEL 滚动（一格一行，无指示条），用户以为不能滚。修：升级一格三行（`±3` 行钳制）；`tags_paint` 末尾补 2px 滚动指示条（右缘 `panel.right-S(8)`，C_HAIR 轨道 + 纯黑滑块，滑块高 = 可视占比×轨道、下限 S(24)，位置随 `t_top` 线性映射）。
   - ⚠️ 教训：改 TagsProc 时先全函数搜过 `WM_MOUSEWHEEL`——第一次插入新 case 直撞 C2196 重复标签（旧 case 一直存在），合并方案 = Ctrl+滚轮不透明度（保留）+ 普通滚轮三行滚动（升级），一个 case 收口。
4. **验证（无头 + 像素断言）**：
   - 网格：真库 13 张 / 14 列单行。像素扫描 v17_panel.png（hair 色 ≈ RGB219 = ARGB(38,17,17,17) 压白）——恰好 12 条竖线（x=97..867 @70px 间距，全部在 13 个实格之间），第 13/14 格缝 (x=937) **无**线，图片行下缘 (y≈280) 以下**零** hair 像素。修前该缝有线穿到空白格。
   - 导入窗：视觉复核——统计行「库 13 张 · 3 个标签 · 2 MB」与按钮排完全分离，整窗无重叠（顺带证实库张数=13 与像素推算一致）。
   - 标签窗：沙盒 40 标签 + 2 条目 → 可见 15 行 + 右缘指示条在轨道顶部（滑块位置正确），底部三按钮正常。
   - 构建 111616B。**真机待验**：滚轮滚标签列表看指示条移动、网格不满行时看线止于末图、导入窗排版。

## 🐛 v0.17.1 复开冻结 + 主面板标签行滚动（2026-09-21，用户真机反馈）

**1. 管理窗第二次打开「冻结」（画面不刷新、点击却生效）——根因是资源生命周期不对称：**
`tags_close/imp_close/set_close` 都做 `ShowWindow(SW_HIDE) + mgr_release()`（释放 DIB 表面，bits=NULL）；复开时 `SetWindowPos` 同尺寸 → **WM_SIZE 不触发** → `*_size`/`mgr_surface` 永不重建表面 → `*_repaint` 撞 `if (!m->bits || !m->gfx) return` 全部早退 → 分层窗一直显示上一会话的旧帧，而点击仍按旧 rects 命中并改设置。首次打开正常只是因为建窗必发一次 WM_SIZE。修复：三处 `*_open` 里 `if (!m->bits) *_size(g_hwnd*);` 就地重建（注意传 g_hwndTags/g_hwndImp/g_hwndSet 本尊，不能透传 open 收到的调用方 hwnd）。教训：**close 里释放的每样资源，open 里必须有对称的重建路径，且不能依赖 WM_SIZE 这类被动触发（同尺寸不触发）**。

**2. 主面板标签行横向滚动**（旧版单行固定，放不下的 chip 直接不画 = 尾部标签永远够不着）：
- 宽度测量从 draw_chip 拆出 `chip_need(label,id)`；panel_repaint 先走**全队列**量宽得 `g_chipMax`（= 全宽 − 可见宽）并当场钳制 `g_chipScr`（标签删减/抽屉开合使可见宽回缩时当帧收敛），再预留溢出侧 S(18) 提示位，`GdipSetClipRectI` 裁剪可见带后落墨。
- draw_chip 只画/只注册**与可见带相交**的 chip（完全滚出左缘的不注册——不在边框拖动死区留不可见命中，901 行死区扫描仍成立）；无论画不画 `*px += need + S(28)` 前进——滚满量必须按全队列计（旧版放不下即停笔，永远算不出滚满量，这是本次的核心反转）。
- 滚轮悬在标签行上 = 行内横滚（±S(100)/格，上=向左），同时 `g_tagFocus = -1`（同鼠标点击的焦点让位语义——可见集变了旧下标失义）；行外滚轮照旧翻网格。
- ‹› 提示（C_INK(150)）：滚过显示 ‹、没到底显示 ›；预留位常驻防布局跳动。
- 无头验证（mp_chips30：30 标签 + 2 条目）：scroll=0 → 「全部」+TAG01..11 + ›、无 ‹；`-scroll 99999` 钳到滚满 → 首见 TAG23、末见 TAG30、‹ 现 › 消。像素断言 + 视觉双确认。

**踩坑两枚（都已记）：** ① PS 的 `& GUI子系统exe` **不等待退出**——Get-Item 在 exe 写完 PNG 前就跑，误报文件不存在；改用 bash 前台或 `Start-Process -Wait`。② 沙盒 index 写**假 hash**（0x1122334455667788）会被 hash_migrate 判变弃条目（580B→436B 全丢）——沙盒条目 hash 必须真实或写 0。

- **v0.17.2 修偏了**：给滚满量「+S(18) 补偿」只把末标签**文字**救完整，但 › 预留位仍然常驻——滚到底后末行永远停在离网格右缘 ~23px 处，与下方图片墙右缘不齐 = 视觉上“最右面还是差一小截”。教训：**预留位/装饰位的存在条件要跟着状态走（“没到底才留”），而不是用滚满量去迁就一个常驻的坑**。
- 修法：`g_chipMax = 裸溢出`（回到朴素值）；右侧裁剪 `clipR = visR0 - (g_chipScr < g_chipMax ? S(18) : 0)`——没到底留 › 位，到底整段放开，末标签盒右缘与 visR0（= 网格右缘）平齐。左缘同构（滚过 0 才留 ‹ 位，v0.17.1 已如此）。
- 无头验证（像素 + 视觉双确认）：末滚 TAG30 完整、行右缘与网格右缘对齐（内容至 x=1148，盒缘 1155=网格右缘，7px 为 chip 自身文字内衬）、无 ›、‹ 在；中途滚 › 在位、chip 于预留位缘裁剪。
- **像素扫描陷阱一枚**：扫“标签带最右内容像素”时先排除面板黑框（x≈1180 的全高竖线）——否则两种滚动状态的 maxX 都是框线，断言失效。构建 112640B。

## 🔧 v0.17.4 标签行竖切半字根治：整枚可见 + 滚轮吸附（2026-09-21，用户真机反馈第四轮）

- **三轮“右面不完整”的真正根因**：原始设计“放不下的 chip 整枚不画”（行尾永远干净，main.c v0.17.0 注释原文）；v0.17.1 加滚动时改成“与可见带相交即画 + GDI+ 裁剪”——从此**任何停留位置**（未滚动溢出时、滚到一半停下时）右缘都可能竖切半个字，中文被竖切格外刺眼。v0.17.2/0.17.3 只修了“滚到底”一个位置，用户停在半途照样切——所以“还是不完整”。教训：**修 bug 前先问“用户停在哪个位置”，别默认停在你修的那个点上；且改行为前先读懂原设计的语义（整枚不画）再动刀**。
- 修法两件套：
  1. **整枚可见门**：draw_chip 只在 `px >= visL && px+need <= visR` 时落墨/注册——缘上绝不竖切；放不下/滚出的整枚略过，行尾留空是原生语义。
  2. **滚轮吸附**：`chip_snap_scroll(cur, dir)` 预演全队列宽度，取“某枚右缘恰齐 clipR”的滚动量为落点（向下取 >cur 的最小候选、兜底滚到底；向上反向、兜底 0）——每次滚一格 = 前进一枚，无中间半字帧。
- 中文标签沙盒（标签01..30，贴近真机数据）三态全验：未滚（末见 标签06 完整 + ›，无竖切）、滚到底（标签30 完整、右缘与网格右缘平齐、‹ 在 › 无、首见 标签23 完整）、任意中途 -scroll 300（首尾标签完整、‹› 并存、全行零竖切）。
- 前几轮的 +S(18) 滚满量补偿已随根因修复回退（g_chipMax = 裸溢出），v0.17.3 的“没到底才留 › 位”保留。构建 113152B。**真机待验**：滚轮逐格滚（每格恰好露出一枚新标签）、任意位置停下看右缘、滚到底平齐、点标签过滤。

## 🐛 v0.17.5 末枚标签永不出现：量宽与画宽不同源（2026-09-21，用户真机反馈第五轮）

- **现象**：v0.17.4 后滚到底只到倒数第二枚（完整），最后一枚永远不出现。
- **根因**：`tag_label()`（main.c:1923）把标签名里的 a-z **大写化**后才落墨——主行 chip 画的是大写文本，而 v0.17.1 的滚满量预演（panel_repaint walk）和 v0.17.4 的吸附（chip_snap_scroll）量的都是**原始名**。含小写字母的标签（anime/kaomoji 之类）实测比画宽窄几像素：缘上裁剪时代误差被裁剪边吃掉看不见；改成“整枚可见门”后，末枚右缘恰超出 visR 几像素 → 被整枚挡掉。**教训：测量与绘制必须同源——凡画的是 f(text)，量宽就必须量 f(text)，不能量 text；两轮沙盒（中文/大写 ASCII）恰好都不受大写化影响 = 测试数据要覆盖数据变换路径（这里是 a-z），不然验证是假阴性。**
- 修法：walk 与 snap 两处 `chip_need` 一律改喂 `tag_label(...)` 后的显示文本。
- 验证（小写 tag01..tag30，画成 TAG01..TAG30，精确复现用户场景）：滚到底**末枚 TAG30 出现**、完整、右缘与图片区对齐、› 消失 ‹ 在；中途 TAG08..16 全完整零竖切。构建 113152B。

## 💥 v0.17.6 滚轮闪退：tag_label 空指针 + 吸附函数无覆盖（2026-09-21，用户真机反馈第六轮）

- **现象**：v0.17.5 一起动标签行滚轮立即闪退。
- **根因**：v0.17.5 修量宽时把吸附循环改成 `tag_label(tagNames[i-1], i-1)` 后才判返回值——但 `tag_label` 用**静态 buf，永不返回 NULL**，判空形同虚设；空标签槽（64 槽位中未占用的，tagNames[i]==NULL）传进去就是 `name[i]` 解引用 NULL → 崩溃。v0.17.4 原逻辑“先读 tagNames 判空再跳过”被改掉了。滚轮是 chip_snap_scroll 的唯一入口，所以只有真机滚轮踩得到。**教训 ×2：① 返回静态缓冲的函数永不为 NULL，“判返回值”代替不了“判输入”；② 只被单一入口调用的函数 = 零覆盖路径，改动它之前先给它铺一条测试通路。**
- 修法：空槽**喂入前**判空跳过；吸附函数改**自包含**——滚满量在函数内按同一 walk 现算（不再读上次绘制留下的全局 g_chipMax，消除首滚/隐藏期吃到陈旧值的隐患）；新增 `-wheelsnap N` 无头钩子逐格预演真链路（`-scroll` 直赋值绕过吸附，测不到这条路）。
- 验证：小写沙盒（30 实标签 + 34 空槽）`-wheelsnap 99` 全程 exit=0 无崩溃，滚到底 TAG30 完整、右缘对齐、› 消失 ‹ 在、零竖切。构建 113152B。**真机待验**：标签行滚轮逐格滚 + 滚到底。

## 🎨 v0.18 分割线移除 + 最近行洗底带（2026-09-21，用户定版）

- **格间发丝线全部移除**（用户：“之前说的分割线我觉得可以全部删掉了”）：draw_grid 的列间/行间发丝线块（含 v0.17 的 lastFill/upB 末行收线逻辑——随主体一起退役）、draw_recents 的最近行列间发丝线；hair_v 助手删除（hair_h 保留，报头横线 g_hairY 与标签窗行线仍用）。网格只靠留白分格。
- **最近行视觉强调 = 整带浅灰洗底**（C_INK(22)，≈230 灰）：带子 [grid.left, wallT−S(4), grid.right, grid.top−S(1)]——左右与图片墙精确对齐、上有 4px 下有 2px 衬边、底缘止于网格顶（不渗进第一行图片缝）；仅有最近记录时出现（g_nRecent>0）。删线 + 成带：层级分隔职责由洗底带接任。
- 验证（18 条目/8 最近沙盒，像素断言）：网格区发丝色列 0 条；带在格缝列 y140..213 连续、跨度 x29..1155 与网格缘齐；两区图片正常渲染。视觉复核通过（视觉模型把最近行格缝透出的带灰误报为“分割线”——像素断言优先）。
- **沙盒踩坑两枚（旧坑新踩）**：① 库文件必须放 `memes\` 子目录——放 exe 根目录 hash_migrate 找不到文件会弃全部条目（index.bin 回写成 16B 空库，.bak 留原件）；② **PS1 中文注释无 BOM 又炸一次**（PS5.1 按 ANSI 读，中文注释破坏后续行解析 → 变量静默为 null）——临时脚本一律纯 ASCII。另外 18 份相同内容图会被迁移判重并成 1 条——要多条目就用“同图 + 尾部各异填充字节”（EOI 后缀解码器忽略、哈希不同）。
- 构建 113152B。**真机待验**：面板整体观感（无线网格 + 灰带最近行）、无最近记录时带子不出现。

## 🎨 v0.18.1 最近行强调改款：洗底带 → 带下墨线（2026-09-21，用户反馈“洗底不好看”）

- **灰洗底带否决**（C_INK(22) 整带铺灰在暖纸白上显脏）。改款：**编辑排版式分节**——最近行下方收一条整宽 1px 墨线（`fill_rect(grid.left, recent.bottom+S(4), grid.right, +1, C_INK(255))`），上有灰 hairY、下有墨线，最近行自成一段；最近行→网格间距 g_gap(3px)→S(8) 给足呼吸。仍仅在 g_nRecent>0 时画。
- 像素断言：洗底带 0 行残留；墨线 y=216、1px 厚、x29..1155 与网格两缘齐；网格图片正常。视觉复核：层级清晰、不突兀。
- 构建 113152B。**真机待验**：观感、无最近记录时不出现墨线。

## 🛡️ v0.18.2 子智能体综合评审 + 修复（2026-09-21，发布前关口）

评审范围 v0.17→v0.18.1 全部改动（标签行滚动六轮、复开冻结、发丝线删除、墨线分节）。核心结论：滚动不变量（量宽/绘制同源经 tag_label、整枚可见门、snap 自包含、空槽先判空、复开三处重建、clip 配对、墨线守卫）**全部核对成立**；发现并修复：

- **[P1] 三处键盘焦点防御钳制可产生 -1 下标且不早退**（最近行最重：`g_recentIdx[-1]` 读到相邻全局的垃圾正数 → Enter 可能发送错条目或越界写 `e->used`；触发链 = 键盘流删光最近项后按 ←/→/Enter）。修：三处钳制后 `<0` 即清焦点 + `panel_repaint(); return 0;`；并让 `recalc_recents` 在 `g_nRecent==0` 时顺手清 `g_recentFocus/g_recentHover`（消除残留行焦点）。
- **[P1-边缘] Enter/Space 选标签后焦点跟随读旧注册集**（激活 chip 变宽 → 可见集变 → 旧下标错位一帧）。修：repaint 后按 id 锚回 + 二连提交（与滚轮 keepId 同款）。
- **[P2] 滚轮标签行判定缺 x 约束**：抽屉开时悬在抽屉上滚轮会滚动被遮住的标签行右半。修：加 `wpt.x < g_gridR`。
- **[P3] 一致性**：文件头版本注释更新为 v0.18.2；draw_grid 过时注释（洗底带→墨线）；chipY 公式三处复制收敛为 `g_chipY` 单一来源（panel_layout 写，绘制/滚轮读）。
- 评审确认无需处理：‹› 不可点（滚轮单通道，by design）、g_chipScr 跨会话保留（无害）、tag_label 63 字符截断（量/画同源自洽）、每帧 ~150 次 MeasureString（交互驱动重绘，可接受）。
- 冒烟：面板/设置/标签三窗渲染正常（标签行、墨线、图片墙无异常）。构建 113152B。

## 🔧 v0.17.2 滚到底末标签被裁 + 滚轮丢键盘焦点（2026-09-21，用户真机反馈）

- **末标签显示不全**：滚满量原 = 全宽 − 可见宽，但落墨带右端被 › 预留位 S(18) 吃掉——滚到底末 chip 右缘永远差 18px 被裁。修：`g_chipMax = 裸溢出 + S(18)`（预留位存在即加回），滚到底末 chip 右缘恰齐裁剪缘、完整显示。沙盒复验：TAG30 完整（右侧留白到边）、‹ 在 › 无、首见 TAG22（末标签全宽入画 → 整体少滚一格，正确）。
- **滚轮清键盘焦点**：原 `g_tagFocus=-1` 一刀切。修为「锚定保留」：滚动前记 `g_chipId[g_tagFocus]`，重绘后仍在新可见集里就按 id 锚回下标并**二次重绘**把焦点线入画（同步两连提交，无中间帧）；滚出视野才清焦点。无头测不了滚轮，真机验证：←←← 聚焦某标签 → 滚一小格 → 焦点线应仍在该标签上；滚到它出视野 → 焦点自然消失。

构建 112640B。
